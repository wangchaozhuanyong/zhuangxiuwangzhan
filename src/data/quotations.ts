import { QuotationItem } from "./types";

/**
 * Local admin quote seed data.
 * Keep this empty so staging/admin previews do not show fake customer budgets or quote amounts.
 */
export const quotationsData: QuotationItem[] = [];
